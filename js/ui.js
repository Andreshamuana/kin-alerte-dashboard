
// Dans renderList() pour le type 'sos'
if (activeTab === 'sos') {
  items = state.sos; // ce sont les alertes
  // Afficher : user, location, status, time
  // Adaptez les champs : alert.userId, alert.location, alert.status, alert.created_at
}
